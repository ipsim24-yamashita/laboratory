// /js/section-cta.js
window.initCTA = function initCTA() {
  const sec = document.getElementById('minicta1');
  if (!sec) return;
  // 例：外部リンクへ rel や analytics を付与する等、必要ならここに
  // console.log("CTA ready");
};
